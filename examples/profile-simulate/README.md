# Profile Simulate Example

Runs a `simulation_profile.v1` file through `client.simulate()` and exits non-zero when any case fails.

```bash
npm install
FEELDKIT_API_KEY=<key> npm start -- ./simulation-profile.json
```
