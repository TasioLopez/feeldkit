# SalesNav To HubSpot Example

Runs the deterministic `linkedin_salesnav__hubspot` flow against a sample record and prints each target field status.

```bash
npm install
FEELDKIT_BASE_URL=http://localhost:3000 FEELDKIT_API_KEY=fk_demo_public_1234567890 npm start
```
