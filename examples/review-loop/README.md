# Review Loop Example

Shows the admin side of the learning loop by undoing a known enrichment proposal decision, then reading the promoted-intelligence registry.

Use only with a disposable proposal ID:

```bash
npm install
FEELDKIT_API_KEY=<admin-key> FEELDKIT_PROPOSAL_ID=<uuid> npm start
```
