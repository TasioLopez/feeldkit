# Curator Queue Example

Lists pending global promotion proposals. Approval and rejection should remain deliberate human actions unless your organization has a separate curation policy.

```bash
npm install
FEELDKIT_API_KEY=<platform-admin-key> npm start
```
