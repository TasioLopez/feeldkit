# Normalize Industry Example

Normalizes a single industry string and prints the canonical label, confidence, review status, and `explain.v1` decision.

```bash
npm install
FEELDKIT_BASE_URL=http://localhost:3000 FEELDKIT_API_KEY=fk_demo_public_1234567890 npm start -- "computer software"
```
